# Server Configuration
server.port=${PORT:8081}

# Database Configuration
spring.datasource.url=jdbc:postgresql://ep-winter-field-a2zw8v0d.us-east-2.aws.neon.tech/walletmate?sslmode=require
spring.datasource.username=kabanofesto
spring.datasource.password=QEEVlGWHJrSQ
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# Connection Pool Settings
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.max-lifetime=1200000
spring.datasource.hikari.auto-commit=true

# SSL Configuration
spring.datasource.hikari.ssl-mode=require
spring.datasource.hikari.connection-init-sql=SET TIME ZONE 'UTC'

# JWT Configuration
jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
jwt.expiration=${JWT_EXPIRATION:86400000}

# CORS Configuration
app.cors.allowed-origins=${ALLOWED_ORIGINS:https://*.onrender.com,http://localhost:5173,http://127.0.0.1:5501,http://localhost:3000}

# Logging
logging.level.org.hibernate.SQL=INFO
logging.level.com.zaxxer.hikari=INFO
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=INFO
